<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:HojozatyCode.ViewModels"
             x:Class="HojozatyCode.Pages.SuccessPage"
             Title="SuccessPage">

    <Grid RowDefinitions="Auto,Auto,Auto,Auto" ColumnDefinitions="Auto">
        <Frame Grid.Row="0" WidthRequest="120" HeightRequest="120" CornerRadius="60" BorderColor="Purple" BackgroundColor="LightGreen" HorizontalOptions="Center">
            <Label Text="✔" FontSize="64" HorizontalOptions="Center" VerticalOptions="Center"/>
        </Frame>

        <Label Grid.Row="1" Text="The Place Was Added"
               FontSize="24"
               FontAttributes="Bold"
               HorizontalOptions="Center"
               Margin="0,20,0,10" />

        <Button Grid.Row="2" Text="My Spaces" BackgroundColor="Purple" TextColor="White" CornerRadius="20" WidthRequest="120" HorizontalOptions="Center" />

        <Button Grid.Row="3" Text="Close" BackgroundColor="Purple" TextColor="White" CornerRadius="20" WidthRequest="120" HorizontalOptions="Center" Margin="0,10,0,0" />
    </Grid>

</ContentPage>